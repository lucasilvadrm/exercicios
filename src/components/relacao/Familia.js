import React from 'react';
import { Text } from 'react-native';

const Familia = ({ children }) => {
  return (
    <>
      <Text>Família</Text>
      {children}
    </>
  );
};

export default Familia;
